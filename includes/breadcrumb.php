<?php
/**
 * Breadcrumbs are intentionally disabled system-wide.
 *
 * Existing module includes remain valid for compatibility, but this shared
 * component renders no markup. Primary navigation, page titles, and explicit
 * back controls provide the application's navigation context.
 */
return;
